---
phase: 1
plan: 1
wave: 1
---

# Plan 1.1: Core Turn Logic Integration

## Objective

Connect `CombatView` to a functional turn-advancement system by syncing Redux state with `CombatEngine` logic.

## Context

- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- .gsd/phases/1/RESEARCH.md
- src/store/slices/combatSlice.ts
- src/engine/combatEngine.ts
- src/components/views/CombatView.tsx

## Tasks

<task type="auto">
  <name>Update combatSlice State & Reducers</name>
  <files>src/store/slices/combatSlice.ts</files>
  <action>
    - Add `turn: number` and `currentActorId: string | null` to `CombatState` interface and `initialState`.
    - Implement `turnAdvanced` logic that correctly increments `turn` index and updates `currentActorId`.
    - Implement `combatStarted` reducer that accepts an array of combatants (id, initiative) and sorts them.
  </action>
  <verify>Check compilation and run vitest on combatSlice (if exists) or verify via logs after next task.</verify>
  <done>Redux state contains turn and currentActorId fields.</done>
</task>

<task type="auto">
  <name>Hook up End Turn button in CombatView</name>
  <files>src/components/views/CombatView.tsx</files>
  <action>
    - Import `turnAdvanced` action.
    - Update the `onClick` handler for the End Turn FAB (line 268) to dispatch `turnAdvanced`.
    - Add visual feedback showing which minion/actor is currently active.
  </action>
  <verify>Open browser and click "End Turn" button.</verify>
  <done>Button dispatches action and updates Redux state.</done>
</task>

## Success Criteria

- [ ] Combat round and turn state is visible in Redux DevTools.
- [ ] "End Turn" button advances turn index and actor ID.
- [ ] Round counter increments when turn cycles.
